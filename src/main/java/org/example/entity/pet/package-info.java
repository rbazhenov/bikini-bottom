/**
 * Пакет содержит сущности с наследованием от обастракного класса,
 * где для родителя не создаётся отдельная таблица в БД,
 * и все наследники дублируют у себя в таблице поля родителя.
 */
package org.example.entity.pet;